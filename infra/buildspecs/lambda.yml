version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - yarn install
  build:
    commands:
      - yarn build:server

artifacts:
  base-directory: .server
  files:
    - '**/*'

cache:
  paths:
    - 'node_modules/**/*'
